
;; Needed for getting lein to exec-path
(add-to-list 'exec-path "/opt/homebrew/bin")

;; Bind right option as ctrl to get control keys for both hands on mac keyboard
(setq mac-right-option-modifier 'control)

(provide 'flowa-mac)
